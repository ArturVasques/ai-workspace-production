# Pinned production dependency lock for Docker builds.
#
# Generated from the dev virtual environment via `pip freeze`, minus
# development/test-only tools (pytest, ruff, mypy, ...) and platform-specific
# packages that do not apply inside the Linux container (pywin32, colorama).
#
# The Dockerfile installs this file BEFORE copying application source, so an
# application code change does not invalidate the dependency install layer.
# Regenerate after changing pyproject.toml dependencies. mypy's own compiled
# runtime pulls in ast_serialize/librt transitive deps that also need
# pruning here (dev-only, not filtered by name pattern below):
#   pip install -e ".[dev]" && pip freeze | grep -v -E "ai-workspace-production|pywin32|colorama|^pytest|^ruff|^mypy|^coverage|^iniconfig|^pluggy|^Pygments|^ast_serialize|^librt"
annotated-doc==0.0.5
annotated-types==0.8.0
alembic==1.20.0
anyio==4.15.1
attrs==26.1.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
click==8.5.0
cryptography==50.0.1
fastapi==0.141.1
greenlet==3.5.6
griffelib==2.3.0
h11==0.16.0
httpcore==1.0.9
httpcore2==2.13.0
httptools==0.8.0
httpx==0.28.1
httpx2==2.13.0
idna==3.20
jiter==0.17.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
Mako==1.4.1
MarkupSafe==3.0.3
mcp==2.2.0
mcp-types==2.2.0
openai==3.14.1
openai-agents==0.22.2
opentelemetry-api==1.44.0
packaging==26.3
pgvector==0.5.0
psycopg==3.3.5
psycopg-binary==3.3.5
psycopg-pool==3.3.1
pycparser==3.0
pydantic==2.13.5
pydantic-settings==2.15.0
pydantic_core==2.46.5
PyJWT==2.14.0
python-dotenv==1.2.3
python-multipart==0.0.32
PyYAML==6.0.3
referencing==0.37.0
requests==2.34.2
rpds-py==2026.6.3
sniffio==1.3.1
SQLAlchemy==2.0.54
sse-starlette==3.4.11
starlette==1.6.0
structlog==26.1.0
truststore==0.10.4
typing-inspection==0.4.4
typing_extensions==4.16.0
tzdata==2026.4
urllib3==2.8.0
uvicorn==0.53.0
watchfiles==1.2.0
websockets==16.1.1
